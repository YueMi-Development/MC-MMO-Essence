name: '${pluginName}'
version: '${version}'
main: org.yuemi.mmoessence.plugin.EssencePlugin
authors: [${authors}]
contributors: [${contributors}]
api-version: '${apiVersion}'
softdepend: [
  YueMiLibs
]
commands:
  essence:
    description: Open the essence stats menu
    usage: /<command>
    permission: essence.gui
